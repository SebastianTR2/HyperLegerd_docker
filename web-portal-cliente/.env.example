# Destino del servicio de datos (puerto típico 3000). Ajuste si el servicio corre en otra máquina o en WSL.
VITE_API_TARGET=http://localhost:3000

# Clave interna del portal (no subir valores reales a Git). En desarrollo use .env.local (no versionado).
# VITE_PORTAL_API_KEY=
