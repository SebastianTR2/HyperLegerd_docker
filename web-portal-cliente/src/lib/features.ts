/** PATCH /clientes/:clienteId habilitado en middleware. */
export const ENDPOINT_EDICION_CLIENTE_HABILITADO = true

/** POST /clientes/:clienteId/baja (baja lógica) habilitado en middleware. */
export const ENDPOINT_BAJA_CLIENTE_HABILITADO = true
