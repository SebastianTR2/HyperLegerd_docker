# Destino del api-middleware (siempre puerto 3000). En la misma Mac use 127.0.0.1, no 169.254.x.x (APIPA).
VITE_API_TARGET=http://127.0.0.1:3000

# Clave interna del portal (no subir valores reales a Git). En desarrollo use .env.local (no versionado).
# VITE_PORTAL_API_KEY=
